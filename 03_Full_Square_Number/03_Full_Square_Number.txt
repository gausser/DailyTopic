题目：一个整数，它加上100和加上268后都是一个完全平方数，请问该数是多少？
程序分析：在10000以内判断，将该数加上100后再开方，加上268后再开方，如果开方后的结果满足如下条件，即是结果。
提示：使用 math 模块。

判断一个数是否为整数：
def is_int(x):
    if x % 1 == 0:
        return True
    else:
        return False
		
is_integer()判断是否为整数

负数的平方也是正数,那最多就是到-100了，所以不能忘了-100